package templates

templ EventsInfiniteScroll(page int, events []Event) {
    <div id="events-list">
        for i, event in events {
            EventCard(event)
        }
        <div hx-get={"/load-more-events?page=" + itoa(page + 1)} hx-trigger="revealed" hx-swap="afterend">
            <p>Loading more events...</p>
        </div>
    </div>
}
